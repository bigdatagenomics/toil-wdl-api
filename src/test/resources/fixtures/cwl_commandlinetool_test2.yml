---
baseCommand: "echo"
inputs:
  example_flag:
    type: "boolean"
    inputBinding:
      position: 1
      prefix: "-f"
outputs:
  example_output:
    type: "File"
    outputBinding:
      glob: "hello.txt"
